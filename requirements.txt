# Runtime dependencies for the Streamlit dashboard (app.py).
# The app renders from committed JSON artifacts, so it needs no ML/SQL/parquet
# libraries. The full pipeline, tests, and CI use requirements-dev.txt.
streamlit>=1.36,<2
plotly>=5.22,<7
pandas>=2.2,<3
